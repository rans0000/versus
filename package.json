{
  "name": "Versus",
  "version": "1.0.0",
  "description": "versus website",
  "scripts": {
    "start": "parcel src/*.html --public-url / --open",
    "build": "parcel build src/*.html --no-cache --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/optimizer-data-url": "^2.5.0",
    "@parcel/transformer-inline-string": "^2.5.0",
    "@parcel/transformer-sass": "^2.5.0",
    "@parcel/transformer-webmanifest": "^2.5.0",
    "parcel": "^2.5.0",
    "sharp": "^0.29.3"
  },
  "dependencies": {
    "swiper": "^8.1.4"
  }
}
